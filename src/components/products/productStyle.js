import { makeStyles } from '@material-ui/core';

const productStyles = makeStyles({
  inline_block: { display: 'inline-block' },
  inline_block_margin: { display: 'inline-block', marginLeft: '32px' }
});

export default productStyles;